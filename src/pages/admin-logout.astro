---
// Logout específico para admin
console.log("🔍 [admin-logout] Iniciando logout de admin");

// Limpiar cookie de admin usando Astro.cookies
Astro.cookies.delete("auth-token", {
  path: "/",
  httpOnly: true,
});

// También limpiar cualquier cookie de usuario público
Astro.cookies.delete("public-auth-token", {
  path: "/",
  httpOnly: true,
});

console.log("✅ [admin-logout] Cookies eliminadas, redirigiendo a login");

// Redirigir al login
return Astro.redirect("/login");
---
